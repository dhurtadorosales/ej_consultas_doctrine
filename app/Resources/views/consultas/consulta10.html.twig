{% extends 'base.html.twig' %}

{% block body %}
    <h1>Listado de grupos</h1>

    <table>
        <thead>
        <tr>
            <th>Grupo</th>
            <th>Número de alumnos</th>
            <th>Tutor</th>
        </tr>
        </thead>
        <tbody>
        {%  for grupo in grupos %}
            <tr>{# comentario #}
                <td>{{ grupo[0].descripcion }}</td>
                <td>{{ grupo[1] }}</td>
                <td>{{ grupo[0].tutor.nombre }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

